SET SERVEROUTPUT ON
ACCEPT AB NUMBER PROMPT 'ENTER A VALUE';
DECLARE
A NUMBER;
PRAGMA EXCEPTION_INT (C,-20009)
BEGIN
A := '&AB';
IF A > 5 THEN
RAISE_APPLICATION_ERROR(-20009,'Enter a number less than 5');
END IF;
DBMS_OUTPUT.PUT_LINE(A);
EXCEPTION
WHEN C THEN
DBMS_OUTPUT.PUT_LINE(SQLERRM);
END;
